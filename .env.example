# Block 14 — BGIN AI
# Copy to .env.local and fill values.

# Mage inference (use one or both; NEAR preferred)
# NEAR key must be from https://cloud.near.ai → API Keys (create key, add credits)
NEAR_AI_API_KEY=your-near-cloud-ai-key
# Model via NEAR (default: openai/gpt-oss-120b to match agentprivacy).
# Other options: anthropic/claude-opus-4-6, deepseek-ai/DeepSeek-V3.1
# NEAR_AI_MODEL=openai/gpt-oss-120b

# NEAR AI Proxy (Cloudflare Worker - fallback if direct API fails)
# Uses same worker as agentprivacy_master
NEXT_PUBLIC_NEAR_PROXY_URL=https://near-ai-proxy.privacymage.workers.dev

# Optional fallback: Anthropic
# ANTHROPIC_API_KEY=sk-ant-...
# ANTHROPIC_MAGE_MODEL=claude-sonnet-4-20250514

# Vector DB (self-hosted)
QDRANT_URL=http://localhost:6333

# Graph DB
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=

# PostgreSQL
DATABASE_URL=postgresql://...

# Optional: Bonfires.ai (alternative knowledge graph)
# BONFIRES_API_KEY=
# BONFIRES_API_ENDPOINT=https://api.bonfires.ai/v1
# KNOWLEDGE_GRAPH_PROVIDER=self-hosted

# Public
NEXT_PUBLIC_APP_URL=http://localhost:3000
