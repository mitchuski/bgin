'use client';
/** 00 Phase 8.4 — Trust network graph (D3). */

export default function NetworkViz() {
  return (
    <div className="rounded-lg border border-[var(--border)] p-6 bg-[var(--bg-secondary)] min-h-[400px]">
      <p className="text-[var(--text-muted)]">Network visualization — WG clusters, anonymized.</p>
    </div>
  );
}
