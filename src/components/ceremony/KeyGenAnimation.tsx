'use client';
/** 00 Phase 2.2 — Key generation visual feedback. */

export default function KeyGenAnimation() {
  return (
    <div className="rounded-lg border border-[var(--border)] p-6 bg-[var(--bg-secondary)]">
      <p className="text-[var(--text-secondary)]">Your Swordsman is forming…</p>
      {/* TODO: crystalline animation per 10_DESIGN_SYSTEM */}
    </div>
  );
}
